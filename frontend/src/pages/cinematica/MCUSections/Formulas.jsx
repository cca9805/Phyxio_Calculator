import React from 'react';
import FormulaCalculator from '../../../components/FormulaCalculator';

export default function MCUFormulasWrapper(props) {
  return <FormulaCalculator topicId="mcu" {...props} />;
}